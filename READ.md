# 이미지 판정 프로그램 (DQE Napari Tool) v16

X-ray Raw 이미지의 노이즈를 보정하고 Blemish(얼룩), 라인 등 불량을 검출 및 분석하기 위한 GUI 프로그램입니다.

## 주요 기능

- **듀얼 뷰어**: 원본(Raw)과 라인 노이즈 보정이 적용된 결과(Corrected) 이미지를 나란히 비교하며 볼 수 있습니다.
- **모델별 설정**: 다양한 검출기 모델(4343, 1616, 1616dyn 등)에 최적화된 이미지 크기, ROI 프리셋, 좌표 보정(회전/반전)을 자동으로 적용합니다.
- **불량 검출**:
  - **Blemish(얼룩)**: 설정된 파라미터에 따라 이미지 내의 얼룩 불량을 검출하여 리스트에 표시합니다.
  - **Line(라인)**: ROI 내에서 저신호 값을 갖는 수직/수평 라인 불량을 탐지하여 오버레이로 표시합니다.
- **CTF 분석**: CTF(Contrast Transfer Function) 패턴 이미지를 분석하여 lp/mm별 CTF 값을 측정하고 결과를 리포트합니다.
- **파라미터 관리**: Blemish 및 라인 검출을 위한 상세 파라미터를 모델/타입별 프로필로 저장하고 관리할 수 있습니다. (`blemish_config.json`)
- **결과 저장**: 보정된 최종 이미지(.IMG) 또는 검출된 Blemish 영역의 확대 이미지(.jpg)를 저장할 수 있습니다.

## 요구 사양

프로그램 실행을 위해 아래 Python 패키지들이 필요합니다.

### 필수 패키지

```shell
pip install "napari[all]" scipy
```

## 실행 방법

```shell
python dqe_v16.py
```

---

## 사용자 가이드

### 1. 기본 분석 (Blemish & Line)

1.  **모델 및 타입 선택**: 우측 패널의 `1. Load` 섹션에서 분석할 이미지에 맞는 `Model`과 `Type`을 선택합니다.
2.  **이미지 로드**: `Load Raw (.IMG/.raw)` 버튼을 클릭하여 원본 이미지 파일을 엽니다.
    -   **1616dyn 모델**: `Load Raw` 클릭 시 DB 폴더와 W 폴더를 순서대로 선택하는 특수 로직이 실행됩니다.
                           DB에서 이미지를 확인 후 Blemish 버튼을 누르면 W 이미지 로딩과 함께 Blemish 검출이 자동으로 수행됩니다.
3.  **자동 처리**: 이미지가 로드되면 아래 작업이 자동으로 수행됩니다.
    -   좌측 'Raw' 뷰에 원본, 우측 'Result' 뷰에 라인 노이즈가 보정된 이미지가 표시됩니다.
    -   모델에 따라 사전 설정된 ROI(노란색 사각형)가 자동으로 그려집니다.
    -   Raw 이미지를 기준으로 Blemish(얼룩) 및 Line(라인) 불량 검사가 자동으로 실행되고 결과가 우측 테이블에 표시됩니다.
4.  **ROI 변경 (선택 사항)**:
    -   새로운 ROI를 그리려면 'Raw' 뷰에서 `Shift` 키를 누른 채 마우스 좌클릭으로 드래그합니다.
    -   ROI를 제거하려면 'Raw' 뷰에서 `Shift` 키를 누른 채 마우스 우클릭을 합니다.
    -   ROI가 변경되면 Result 이미지와 불량 검출이 자동으로 업데이트됩니다.
5.  **수동 검출**: `2. Result` 섹션의 `Detect Target`에서 'Raw' 또는 'Corrected'를 선택한 후 `Blemish` 버튼을 누르면 해당 이미지를 기준으로 수동 검사를 실행합니다.
6.  **결과 확인**:
    -   검출된 불량은 우측 테이블에 리스트업됩니다.
    -   Blemish는 'Result' 뷰에 텍스트 라벨로 위치가 표시됩니다.
    -   Line은 'Result' 뷰에 주황색(일반) 또는 노란색(3-line/Broken) 오버레이로 표시됩니다.
    -   테이블에서 Blemish 항목을 클릭하면 `3. Crop Preview` 뷰에 해당 영역의 확대 이미지가 나타납니다.

### ※ 예외: 1616dyn 모델 사용 방법

`1616dyn` 모델은 단일 파일을 로드하는 대신 여러 이미지의 평균을 내어 분석하므로 아래의 특수 절차를 따릅니다.

1.  **모델 선택**: `Model` 드롭다운에서 `1616dyn`을 선택합니다.
2.  **`Load Raw` 실행**:
    -   `Load Raw` 버튼을 클릭합니다.
    -   **DB 폴더 선택**: 먼저 Dark Bias(DB) 이미지들이 들어있는 폴더를 선택하라는 창이 뜹니다. 해당 폴더를 선택합니다. 폴더 내의 모든 `.IMG` 파일들의 평균 이미지가 계산됩니다.
    -   **W 폴더 선택**: 다음으로 White(W) 이미지들이 들어있는 폴더를 선택하라는 창이 뜹니다. 해당 폴더를 선택합니다. (W 폴더는 선택 사항입니다.)
3.  **분석**:
    -   폴더 선택이 완료되면 DB 이미지들의 평균이 'Raw' 뷰에 표시되며 기본 분석이 자동으로 수행됩니다.
    -   이 상태에서 `Blemish` 버튼을 **처음 누르면**, 'Raw' 뷰의 이미지가 W 이미지들의 평균으로 전환되며 다시 분석을 수행합니다.

이후의 분석 및 조작 방법은 다른 모델과 동일합니다.

### 2. CTF 분석

1.  **기준 이미지 로드**: `Load Raw` 버튼을 눌러 CTF 분석의 기준이 될 White 이미지를 먼저 로드합니다.
2.  **CTF 이미지 로드**: `Load CTF` 버튼을 눌러 분석할 CTF 패턴 이미지를 로드합니다. 이미지가 'Raw' 뷰에 나타납니다.
3.  **분석 라인 지정**: 'Raw' 뷰(CTF 이미지) 위에서 분석할 라인의 시작점과 끝점을 마우스 **가운데 버튼(휠 클릭)**으로 두 번 클릭하여 지정합니다. 노란색 라인이 표시됩니다.
4.  **CTF 분석 실행**: `CTF` 버튼을 누릅니다.
5.  **범위 선택**: 라인 프로파일이 그려진 별도의 그래프 창이 나타납니다. `1lp MAX`, `1lp MIN`, `2lp MAX`... 순서에 따라 그래프에서 마우스 드래그로 각 구간을 선택합니다.
6.  **결과 확인**: 모든 구간 선택이 완료되면 CTF 계산 결과창이 나타납니다. 결과는 클립보드에 자동으로 복사되며, CTF 이미지와 같은 경로에 `.txt` 파일로 자동 저장됩니다.

### 3. 파라미터 및 저장

-   **파라미터 수정**: `Param` 버튼을 누르면 Blemish 및 라인 검출에 사용되는 모든 파라미터를 수정할 수 있는 창이 열립니다. 파라미터는 현재 선택된 `Model`/`Type` 프로필에 자동으로 저장됩니다.
-   **결과 이미지 저장**: `Save Result` 버튼으로 현재 'Result' 뷰의 이미지를 16비트 `.IMG` 파일로 저장할 수 있습니다.
-   **크롭 이미지 저장**: 테이블에서 저장할 Blemish들을 선택(다중 선택 가능)한 후 `Save Crop` 버튼을 누르면, 각 Blemish의 확대 이미지를 지정한 폴더에 `.jpg` 파일로 저장합니다.

---

## 뷰어 조작 (마우스 및 키보드)

| 동작 | 기능 | 해당 뷰어 |
| :--- | :--- | :--- |
| **마우스 좌클릭 + 드래그** | 이미지 이동 (Pan) | Raw, Result, Crop |
| **마우스 우클릭 + 드래그** | 명암 조절 (Window/Level) | Raw, Result, Crop |
| **마우스 휠** | 확대 / 축소 | Raw, Result, Crop |
| **마우스 좌클릭 더블클릭** | 자동 명암 조절 | Raw, Result, Crop |
| **마우스 가운데 클릭 (휠 클릭)** | **[일반 모드]** 뷰 초기화 (줌/팬/명암) | Raw, Result, Crop |
| | **[CTF 모드]** CTF 분석 라인 포인트 지정 | Raw |
| **`Shift` + 좌클릭 드래그** | ROI 그리기 | Raw |
| **`Shift` + 우클릭** | ROI 제거 | Raw |

---

## 설정 파일

-   **`blemish_config.json`**: Blemish/라인/CTF 검출 파라미터가 모델 및 타입별 프로필로 저장되는 파일입니다. 프로그램을 종료해도 설정이 유지됩니다.
-   최초 실행 시 이 파일이 없으면 기본값이 포함된 설정 파일이 자동으로 생성됩니다.